# Node modules (Must never be pushed)
node_modules/

# Expo files and local builds
.expo/
dist/
web-build/

# Automatically generated TypeScript declarations
expo-env.d.ts

# Native build directories (generated if you run prebuild)
android/
ios/

# Local environment variables and secrets (API keys, passwords)
.env
.env.local
.env.*.local

# macOS system files
.DS_Store

# Windows system files
Thumbs.db
ehthumbs.db
Desktop.ini

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
